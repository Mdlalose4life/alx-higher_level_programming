-- Delete the score that is below 5.
-- All the records below 5 must be removed.
DELETE FROM second_table WHERE score <= 5;-- Delete the score that is below 5.
-- All the records below 5 must be removed.
DELETE FROM second_table WHERE score <= 5;
