-- Calculate the avarage.
-- Calc the avr of the score in second_table.
SELECT AVG(score) FROM second_table;-- Calculate the avarage.
-- Calc the avr of the score in second_table.
SELECT AVG(score) FROM second_table;
